.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
node_modules
dist
coverage

# @pmndrs/docs local build output (yarn docs / yarn docs:build)
docs/out

# Claude Code agent worktrees and local session state
.claude
