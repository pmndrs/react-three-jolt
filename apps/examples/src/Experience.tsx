// Doing things inside an experience makes sure we stay in r3f scope
// maybe with routing this is a bad idea

import { Placeholder } from '@react-three/jolt';
import { PhysicsSystem } from '@react-three/jolt';

export function Experience() {
  return (
    <>
      <Placeholder />
    </>
  );
}
